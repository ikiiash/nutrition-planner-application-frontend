name: nutrition-planner-application-frontend

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
